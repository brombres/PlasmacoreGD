module Geometry
uses Graphics
uses RogueGD

augment Box
  METHODS
    method fill( color:Color )
      RogueGD.gd_plasmacore.fill( this, color )
endAugment

augment IntBox
  METHODS
    method fill( color:Color )
      RogueGD.gd_plasmacore.fill( this, color )
endAugment
